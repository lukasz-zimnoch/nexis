# Required environment variables — no defaults. Get these from
# Firebase Console → Project settings → Your apps (Web SDK config).

VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
